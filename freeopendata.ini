[uwsgi]
project = FreeOpenData
base = /home/main

chdir = %(base)/%(project)
home = %(base)/%(project)/env
module = freeopendata.wsgi:app

master = true
processes = 5

socket = %(base)/%(project)/freeopendata.sock
chmod-socket = 666
vacuum = true